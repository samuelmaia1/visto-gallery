import { View } from "react-native";
import { HomeScreenNavigationProp } from "../nav/RootParam";

export function Home({navigation} : HomeScreenProp) {
    return (
        <View>
            
        </View>
    )
}

interface HomeScreenProp {
    navigation: HomeScreenNavigationProp
}